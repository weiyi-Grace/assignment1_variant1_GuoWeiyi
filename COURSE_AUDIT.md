# 课件知识点应用检查

检查依据：主目录 Lecture 1–6 课件、Assignment 1 的 Variant 1 要求、train.py、保存的模型与输出。下面页码均为 PDF 阅读器中的物理页码，部分课件页脚编号不同。

结论：核心训练与评价流程正确，未发现影响当前指标的实现错误；原先的数据探索和部分说明不够充分。已补充训练集探索及独立指标核对，但没有改变模型、参数或重新根据测试集选模型。最终提交 PDF 和 GitHub 链接仍未完成。

## 逐项对照

| 课件知识点 | 课件位置 | 当前实现与判断 |
|---|---|---|
| 分类任务、预测目标 | Lecture 1；Lecture 4 | label 为已知二元标签，采用监督分类，正确；不是聚类或关联规则任务。 |
| 属性类型 | Lecture 2，第 8–11 页 | 7 个数值列和 8 个字符串类别列分别处理。数据类型不等于测量尺度；没有数据字典，不能宣称每个数值变量都是比例尺度。geological_era 名称可能有自然年代顺序，目前 one-hot 是不施加顺序约束的保守建模选择，不能说所有字段天然都无序。 |
| 缺失、重复、异常 | Lecture 2，第 26、30、32 页 | 数值中位数、类别众数填补；目标缺失行排除；训练重复行去重；IQR 标记但保留极值。与“依据任务作出清洗决定”一致。没有证明缺失完全随机，删除缺失标签仍有潜在选择偏差。 |
| 频数、位置与离散程度 | Lecture 3，第 9–13 页 | 原有 describe、标签频数和直方图；现补充每个类别的频数/占比/yes 比率，以及按标签分组的数值统计。 |
| 箱线图、散点图、多变量关系 | Lecture 3，第 5、22–25、53、56 页 | 原先不足，现补充箱线图、数值相关矩阵和训练集分层抽样散点图。全部只使用训练数据；不据此重新选择测试表现更好的模型。 |
| 标准化 | Lecture 2，第 57、59 页 | 逻辑回归数值输入标准化，决策树不缩放，正确。z-score 得到近似零均值/单位标准差，并不会让任意分布变成正态分布。 |
| 类别编码 | Lecture 2，第 59 页；Lecture 4，第 17–19 页 | 使用 one-hot；sklearn 当前采用数值阈值分裂，直接给无序类别编号会限制可用分裂方式。one-hot 合理，但树的一次分裂通常是某类别 vs 其他类别，不等价于原生搜索任意类别子集。 |
| 特征选择 | Lecture 2，第 51–52 页 | 保留全部原始特征，有理由；树分裂具有嵌入式特征选择性质。L2 主要收缩系数，不能宣称它执行了稀疏特征筛选。作业把特征选择/工程列为 if applicable，不要求一定删列或做 PCA。 |
| Gini 与树生长 | Lecture 4，第 26–33 页 | DecisionTreeClassifier 使用 Gini，按阈值贪心二分，正确。使用 balanced 权重时，是加权类别比例对应的 Gini，不是未经加权的原始计数比例。 |
| 过拟合与预剪枝 | Lecture 4，第 49–59 页 | max_depth、min_samples_leaf 限制复杂度；同时保存训练/CV/测试成绩，正确。这是预剪枝，没有实施后剪枝、REP 或 MDL。 |
| 超参数与实验设置 | Lecture 5，第 8–10 页 | C、树深度、叶节点样本数和类别权重用于调参；seed、折数和并行数属于实验设置。GridSearchCV 在训练集内部搜索，正确。 |
| Holdout、分层与交叉验证 | Lecture 4，第 81 页 | 使用已给定 train/test 划分，在训练集进行 5 折分层交叉验证。无需强行改成课件举例的 2:1 比例。全部拟合型预处理置于折内，未发现目标列进入特征或测试集参与调参。 |
| 混淆矩阵与 F1 | Lecture 4，第 72–77 页 | yes 是正类；矩阵输出按 no、yes 排列，因此为 [[TN,FP],[FN,TP]]，与课件按 yes、no 排列不同但数学含义一致。已从逐行预测独立重算并核对指标。 |
| ROC 与 PR | Lecture 4，第 83–84 页 | 使用连续预测概率绘图/计算 ROC-AUC，使用默认决策标签计算 F1，正确。PR 汇总输出 average precision，不能与梯形积分 PR-AUC 混称。 |
| 学习曲线 | Lecture 4，第 80 页 | 未绘制。属于可选诊断，不是题目明文必做项。训练与测试分数比较不是学习曲线，调参分数随树深度变化也不是训练样本量学习曲线。 |
| 替代分类器 | Lecture 5，第 3 页及后续 | 详细讲授 KNN、规则、Bayes、集成分类器；逻辑回归只是 Lecture 2 第 59 页提到。之前将两个模型笼统称为“课程中涉及”不够精确。作业允许任意两个现有分类器，因此无需为合规而更换。 |
| 关联规则 | Lecture 6 | Apriori、support、confidence、lift 与本次监督分类目标不同；没有使用不属于遗漏。 |

## 新增探索发现

训练集 Pearson 相关系数：performance_score 与 stability_index 为 0.8911，与 composite_rank 为 0.8799；stability_index 与 composite_rank 为 0.8299。存在较强成对线性相关，不能再笼统地说没有冗余风险。

当前保留这些字段、使用 L2 正则化是合理选择，但 L2 不会消除相关性。系数可能在相关特征间分配，不能据系数绝对大小直接断定单个变量的独立作用。相关矩阵不是完整的多重共线性检验，也不证明因果关系；若以后做删列/PCA 对照，应在训练折内完成并重新安排独立评价。

训练集中 aptitude_score 的 no/yes 中位数分别为 324.52 / 401.315，composite_rank 为 2.989 / 3.755，展示了组间分布差异。这支持探索预测关系，但不能推出某个分数导致标签变化。类别频数和 yes 比率同样只是描述性证据，低频类别的比例会更不稳定。

## 核查范围与实际限制

- 已运行 check_coursework.py：从原 CSV 核对有效测试行及真实标签；重新加载两个模型，对照所有 13,333 行预测与概率；用 TP/TN/FP/FN 独立重算 accuracy、balanced accuracy、precision、recall 和 F1；核对 CV 最优分数与保存的网格结果。
- 本次没有重新训练或改动调参网格；未做学习曲线、后剪枝、PCA、SMOTE、阈值优化或显著性检验。这些方法并非需要全部叠加的流水线步骤。
- 字段含义和产生时间未知，不能从代码检查排除语义上的目标泄漏、实体关联或时间依赖；分层随机 CV 以样本近似独立为前提。
- balanced 类别权重会改变拟合目标，输出的 yes 概率不应未经校准就解读为现实人群的真实发生率。当前只用于排序评价与分类，不声称概率已经校准。
- 本次添加的探索发生在初次实验之后，应如实称为补充检查，不能在报告中写成最初已经依据这些图选择了模型。
- 作业仍要求两页以内报告和 GitHub 链接；代码及这些说明文件不能替代最终报告。

## 如何复现补充检查

在 D:\msis\data mining 打开 PowerShell：

```powershell
.\.venv\Scripts\python.exe .\assignment_variant1\check_coursework.py --data-dir "D:\dataset"
```

读取 outputs 中的 category_frequencies.csv、numeric_by_class.csv、numeric_correlations.csv/png、boxplots_by_class.png、training_scatter.png 和 verification.json。
