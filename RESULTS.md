# 实际运行结果与报告素材

以下数字来自本目录代码在提供的 train.csv、test.csv 上的实际运行，非示例值。

## 数据与处理

训练集原始 31,112 行，删除 3 行缺失标签后使用 31,109 行：no 23,645，yes 7,464（约 24.0%）。测试集原始 13,334 行，1 行缺失标签不计分，最终评估 13,333 行：no 10,165，yes 3,168。共 15 个特征，其中数值 7 个、类别 8 个。训练集和测试集均无完全重复行，未发现跨集合完全相同的特征行；训练集没有同一特征组合对应不同标签的情况。

训练集各特征存在 1–7 个缺失值，采用数值中位数、类别众数填补。数值范围和分布差异明显，逻辑回归做标准化。类别使用 one-hot，避免给地区、乐器等变量人为赋予顺序。IQR 检查发现 activity_duration 有 5,347 个标记值、load_ratio 有 2,526 个标记值；这些仅为统计异常，未直接删除。没有数据字典支持将其视为测量错误。

## 参数与验证

使用训练集 5 折分层交叉验证，seed=42，以 yes 类 F1 选参数；填补、编码和缩放均在折内拟合。交叉验证后重新拟合全部有效训练数据，然后评估测试集。

| 模型 | 最佳参数 | 交叉验证 F1 均值 ± 折间标准差 |
|---|---|---|
| 逻辑回归 | C=0.1，class_weight=balanced | 0.6608 ± 0.0032 |
| 决策树 | max_depth=8，min_samples_leaf=50，class_weight=balanced | 0.6407 ± 0.0065 |

逻辑回归在 71 次迭代后收敛，未达到 3,000 次上限。最终决策树深度为 8、叶节点为 149。逻辑回归搜索 8 组参数，树搜索 30 组参数，共 190 次交叉验证拟合，另各自进行一次最终重拟合。

## 独立测试集比较

| 模型 | Accuracy | Balanced accuracy | Precision (yes) | Recall (yes) | F1 (yes) | ROC-AUC | Average precision |
|---|---:|---:|---:|---:|---:|---:|---:|
| 多数类基线 | 0.7624 | 0.5000 | 0.0000 | 0.0000 | 0.0000 | 0.5000 | 0.2376 |
| 逻辑回归 | 0.7832 | 0.8033 | 0.5274 | 0.8415 | 0.6484 | 0.8879 | 0.6979 |
| 决策树 | 0.7683 | 0.7967 | 0.5074 | 0.8507 | 0.6357 | 0.8749 | 0.6597 |

逻辑回归混淆矩阵（行=真实 no/yes，列=预测 no/yes）：`[[7776, 2389], [502, 2666]]`。

决策树混淆矩阵：`[[7549, 2616], [473, 2695]]`。

## 结论与讨论

补充课件检查发现，训练集 performance_score 与 stability_index 的 Pearson 相关为 0.8911，与 composite_rank 的相关为 0.8799。保留特征并使用 L2 正则化有合理性，但不能称为已消除多重共线性，亦不宜把单个回归系数解释为独立因果效应。这是初次建模之后补充的探索，并未据此修改已评估的模型。

依据训练集交叉验证预先选择逻辑回归，测试结果也支持该选择：其 F1、ROC-AUC 和 average precision 都高于决策树。逻辑回归全训练集 F1=0.6621，测试 F1=0.6484；树对应 0.6597 和 0.6357。两者存在一定泛化差距，但不能仅凭这一次实验断言出现严重过拟合或统计显著差异。

多数类预测虽然有 76.24% 准确率，却完全无法识别 yes，说明仅报告准确率会造成误导。采用 balanced 类别权重的两个模型均获得超过 84% 的正类召回，但正类精确率只有约 51%–53%，存在较多误报。决策树比逻辑回归少漏判 29 个 yes，同时多误报 227 个 no，因此推荐逻辑回归作为本次综合表现更好的模型；若漏判成本特别高，则需要根据明确业务代价重新设计评价方式，并只在训练/验证数据上调整阈值。

决策树能够表达非线性交互，但本次受限树没有优于正则化线性模型；这并不证明数据不存在非线性。没有额外制造特征，因为缺乏领域依据。未来可以在新的训练/验证实验中探索交互项或其他模型，但不能反复根据当前测试集分数调参后仍声称它是独立测试集。

提交时建议将数据与清洗、模型配置、上表和结论压缩成两页以内的英文报告。请补上本人姓名、学号及真实 GitHub 链接。本文件是中文结果素材，不是最终提交 PDF。
